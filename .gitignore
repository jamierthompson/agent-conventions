.DS_Store
**/.DS_Store

node_modules/

# agent-team worktrees are full repo checkouts under a runtime dir — never commit them
.claude/worktrees/

# runtime-owned, personal, or secret-bearing
.claude/settings.local.json
.env*
!.env.example
